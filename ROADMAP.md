# ROADMAP.md — badmintul.com

Status values used below: **Not started** / **In progress** / **Complete** / **Blocked**.
This file tracks phase-level status only. Task-level detail and acceptance criteria live in `TASKS.md`. Update the status column here whenever a phase's task set is entirely done (Complete), partially done (In progress), untouched (Not started), or stuck on an external/decision dependency (Blocked — note the blocker inline).

| Phase | Goal | Status | Depends on |
|---|---|---|---|
| 0 — Repo & Environment Setup | Repo structure, `app/` scaffold, Convex project, Hostinger access confirmed | In progress — `app/` scaffold + Convex project done (Tasks 1–2); Hostinger access still not confirmed | — |
| 1 — Data Model & Auth | Convex schema for users/roles/venues/courts/bookings; auth wired end to end | Complete for dev, correcting a prior overstatement — schema (Task 3) and role-check *logic* (Task 4) were genuinely verified, but real sign-in through a browser was broken from Task 4 onward until Task 27 (2026-09-18) fixed missing `convex/http.ts`/JWKS/SITE_URL wiring; every earlier "verified" claim here was true only at the `convex-test`/`--identity` level, which bypasses real auth entirely. Production deployment has not yet been checked for the same issue. R-3 decision recorded in RISKS.md 2026-09-17 | Phase 0 |
| 2 — Superadmin Core | Superadmin login, venue approval queue, platform metrics view | Complete — approval queue/metrics/role-gating (Task 6), distinct `/admin` route with redirect-on-mismatch (Task 13), and a repeatable CLI bootstrap script + runbook (Task 14) all implemented and independently verified 2026-09-17 | Phase 1 |
| 3 — Venue Owner Core | Venue owner signup, venue/court creation, pending-state UX | Complete — Task 5 implemented and independently verified, including the R-8 owner-isolation negative test | Phase 1 |
| 4 — Player Core | Browse venues, live availability calendar, booking flow, booking history | Complete — Tasks 7–8 implemented and independently verified, including the R-4 concurrent-booking negative test | Phase 1, Phase 3 (needs at least one approvable venue) |
| 5 — PWA & Landing Page | Public landing page, manifest + service worker, installability, offline app shell | In progress — landing page `/login` link fixed 2026-09-17, `npm test` now passes clean (21/21); PWA (Task 10) manifest/service-worker is solid but Android/iOS device install verification (R-5) is still outstanding and blocks calling this phase fully Complete | Phase 4 (needs real app screens to wrap) |
| 6 — Deployment | Production Convex deployment, Hostinger static hosting, domain + DNS + HTTPS on badmintul.com | Blocked — Convex production deployment exists, but Hostinger upload/domain/HTTPS (R-1, R-7) has not happened; no hosting credentials available in this environment | Phase 5 |
| 7 — Hardening & Launch Check | Double-booking race test, role-boundary audit, cross-device PWA install test | Complete for what doesn't need Hostinger — cross-role audit and R-4/R-8 re-tests (Task 12a) now run and confirmed against actual production Convex (2026-09-17), not just the local simulator; cross-device PWA install (R-5) still needs a physical device | Phase 6 |
| 8 — Route Architecture, Admin Operability & Deploy Readiness | Real per-role dashboard routes (Task 13), repeatable superadmin bootstrap (Task 14), WIB-explicit availability (Task 15), Hostinger deploy runbook prep (Task 16) | Complete — Tasks 13–16 implemented and independently reviewed 2026-09-17; the two follow-up gaps (Task 13a stale `Home.tsx`, Task 15a leftover browser-local timestamp) were fixed and independently re-verified the same day | Phase 2, Phase 7 (closes gaps those phases left open) |
| 9 — UI/UX Polish | Shared styled UI primitives (Task 17), form loading/error states (Task 18), a real booking calendar/grid (Task 19), dashboard loading/empty states (Task 20), accessibility & responsive audit (Task 21) | Complete — Tasks 17–21 implemented and independently verified 2026-09-17, including hand-recomputed WCAG contrast ratios for the darkened brand-accent/success/warning shades (all ≥4.83:1, clear of the 4.5:1 AA minimum). No gaps found in this phase itself | Phase 4 (needs the booking/browse/dashboard screens to exist); Task 20 correctly landed after Phase 8's Task 13 |
| 10 — Venue Owner Operations & Superadmin Moderation | Court availability blocking (Task 23), venue owner bookings view (Task 24), venue owner revenue/utilization stats (Task 25), superadmin suspend venue/user (Task 26) | Complete — Tasks 23–26 implemented and independently reviewed 2026-09-18; the one follow-up gap (Task 26a, suspended-user dashboard crash) was fixed and independently re-verified the same day, centralized in `RoleDashboard.tsx` so none of the three dashboard panels needed individual changes | Phase 3, Phase 4 (needs venues/courts/bookings to exist); Phase 2 for Task 26's superadmin surface |
| 11 — Navigation Shell & Auth/Flow Redesign | Persistent authenticated-route header with a reliable sign-out (Task 28), branded/clearer auth screens (Task 29), post-login/post-logout transition feedback (Task 30), dashboard layout pass (Task 31) | Complete — Tasks 27's production follow-up and 28–31 implemented and independently verified live in a real browser 2026-09-18; the one follow-up gap (Task 28a, sign-out landing on `/login` instead of `/` due to a redirect race) was fixed and stress-tested with 3 full login/sign-out cycles the same day, all landing on `/` | Phase 2, Phase 3, Phase 4 (needs the dashboard routes and screens to exist) |
| 12 — Commercial Design System | Commercial design tokens, shared UI components, responsive layout system, and visual QA | Complete — Tasks 32–35 and follow-up Task 33a all implemented and independently verified (2026-09-18/19). Phase and tasks were self-planned and self-implemented by Codex in one unreviewed pass (per the user: "accidentally asked codex to plan phases and tasks"), not assigned by Claude Code's planning role — accepted on technical merit after review, but flagged as a process gap (see REVIEW.md's process note above Task 32). Task 33's original gap (12 UI components built but completely unused) was fixed by Task 33a: unused ones deleted, only genuinely-used primitives remain | Phase 9, Phase 11 |
| 13 — Public Marketing & Conversion | Conversion-focused landing page, role-specific value sections, trust content, venue discovery, support navigation, and SEO/social metadata | Complete — Tasks 36–41 implemented and independently verified live in a browser 2026-09-18, including honest placeholder policy pages (no fabricated legal text) and no fabricated trust content (no fake testimonials/ratings). Same self-planned/self-implemented process note as Phase 12 applies here too | Phase 12 |
| 14 — Dashboard Shell: Sidebar Navigation & Modern App Layout | Sidebar + topbar shell (Task 42), split dashboard panels into sidebar-navigable sections (Task 43), topbar user menu (Task 44), shell visual QA (Task 45) | Complete — Tasks 42–45 and follow-ups 43a/44a/44b all implemented and independently verified live 2026-09-19; a genuine visual transformation (real sidebar, active-link highlighting, topbar user menu). Task 44b replaced the broken `aria-hidden` approach with the real `inert` attribute plus a viewport-aware `matchMedia` check, fixing the severe desktop regression — verified live that the sidebar is no longer hidden from screen readers at desktop width while remaining genuinely interactive there | Phase 8 (routing), Phase 9 (design tokens/primitives), Phase 11 (AppShell) |
| 15 — Sidebar Coverage: Expose Existing Backend Gaps | Player "My Bookings" + cancel (Task 46), venue owner availability/maintenance management (Task 47), superadmin venue moderation (Task 48), superadmin user moderation (Task 49) | Nearly complete — Tasks 46–49 and follow-up 49a implemented and independently verified 2026-09-19/20. Task 49a's self-suspension fix is genuinely correct and verified live (no functioning suspend control at all on the caller's own row). Task 47a is only 2/3 done: `deleteBlock` and the Users-view suspend button got pending states, but the Venues-view suspend button was missed entirely — its handler tracks the pending state correctly, but the button's JSX was never updated to use it. **Task 47b** finishes that one control — **on hold, Codex usage limit exhausted until 2026-09-21** | Phase 4 (booking), Phase 10 (court blocks, suspension), Phase 14 (sidebar shell to extend) |
| 16 — Complete Superadmin Dashboard | Platform-wide bookings view (Task 50), platform settings infrastructure (Task 51), configurable cancellation window (Task 52), configurable booking lead-time with real server-side enforcement (Task 53), supported cities (Task 54) | Not started — added 2026-09-20, planned by Claude Code against `SPEC.md` §1's superadmin story. Two real gaps found: no platform-wide bookings view exists at all, and global settings (explicitly deferred by Task 6) were revisited by direct request now that the rest of the superadmin surface is complete. Task 53 also closes a real pre-existing gap: `createBooking` currently has no server-side lead-time enforcement at all, only a client-side UI clamp | Phase 4 (booking, R-4 atomicity), Phase 3 (venue submission), Phase 15 (superadmin sidebar to extend) |

## Notes on sequencing

- **Phase 4 depends on Phase 3**, not just Phase 1, because a player needs at least one real, approved venue with courts to book against — building the player flow against pure mock data risks schema drift from the venue-owner flow. Build venue owner core first, seed one approved venue, then build player flow against real data.
- **PWA work (Phase 5) is deliberately last before deployment**, not first. Wrapping unfinished screens in a service worker early creates cache-invalidation debugging overhead for no benefit — do it once the screens are stable.
- **Nothing in Phases 0–5 depends on Hostinger being reachable.** Local dev against a Convex dev deployment works standalone; Hostinger only enters the picture at Phase 6. Don't let Hostinger account/FTP setup block earlier phases — that's why it's scoped to Phase 0 as "access confirmed" only (not full deploy pipeline).
- **Phase 8 exists to close gaps found on review, not to add scope.** Tasks 13–15 fix real debt (routing, admin bootstrap, timezone correctness) identified while independently verifying Phases 2 and 7 — none of them were required to make the app *work*, but all three would be more expensive to fix after Hostinger deploy than before it. Task 16 is pure documentation and has zero risk of blocking anything; it exists so Task 11/12a criterion 4 can be executed quickly the moment Hostinger credentials arrive, instead of being figured out live. Do Task 13 first (routing touches the most surface area); 14 and 15 can happen in either order; 16 last.
- **Phase 9 is presentation-layer only, deliberately separated from Phase 8's structural work.** Every Convex function, role check, and double-booking guard verified in Phases 1–7 is untouched by Phase 9 — its tasks (17–21) explicitly forbid touching `convex/*.ts` logic, keeping the risk profile low even though it touches nearly every screen. Do Task 17 (shared primitives) first, since 18–20 all consume it; touching every form/list twice (once without primitives, once to retrofit them) is wasted work. Task 20 specifically should wait for Phase 8's Task 13 if that hasn't landed yet, since both touch the same dashboard panel files — check current state before starting rather than guessing which order causes less rework. Task 21 (accessibility/responsive audit) is last because it audits the *outcome* of 17–20.
- **Phase 10 touches `convex/*.ts` again, unlike Phase 9 — treat it with the same rigor as Phases 1–7, not Phase 8/9's lighter cleanup review.** Task 23 in particular adds a new atomicity-sensitive check (court blocks) to the existing booking mutation R-4 already governs — this is exactly the kind of change CLAUDE.md's review checklist item 2 (verify a real negative test, not just UI hiding) and item 4 (does this touch a BLOCKING risk) exist for. Do Task 23 first since Task 24's bookings view and the availability display both benefit from blocks being modeled; 24 and 25 can happen in either order after that; Task 26 (suspension) is independent and can happen anytime, but doing it last means its role-check changes only need to be threaded through the codebase once.
- **Process lesson behind Phase 11 and Task 27, worth internalizing:** every prior "independently verified" claim in this file about auth was true only at the `convex-test`/CLI-`--identity` level, which injects a fake session directly and never exercises real JWT issuance, signing, or discovery. That layer proves authorization logic is correct *once a session exists* — it says nothing about whether a session can ever be created. Task 27 (found by finally opening a real browser) shows that gap can hide a launch-blocking bug through nine review passes and 53 passing tests. Phase 11 itself is presentation-only (like Phase 9) and doesn't touch `convex/*.ts` — do Task 28 first (fixes the confirmed sign-out dead end and gives 29/31 a shell to build inside), then 29/31 in either order, Task 30 whenever convenient.
- **Phase 15 exists because a sidebar redesign (Phase 14) surfaced backend work that was already done but never exposed.** `cancelBooking`, `createCourtBlock`/`removeCourtBlock`, and `setVenueSuspended`/`setUserSuspended` were all built, correct, and tested in Phases 4/10 — but nothing in the UI ever called them. Unlike Phase 14, this phase touches `convex/*.ts` (three new read-only queries: an owner's court blocks, all venues for superadmin, all users for superadmin) — treat it with Phase 1–7/10 rigor, especially Task 49's `listUsers` query, which must stay superadmin-gated and minimal (`email`/`role`/`suspended` only). Do Task 46 first (no new Convex query, lowest risk), then 47, then 48/49 in either order.
- **Phase 16 revisits a deliberate earlier scope decision (Task 6's deferral of global settings) by direct request — this is a legitimate scope change, not scope creep discovered on review.** It's also the largest `convex/*.ts` change since Phase 1: a new table, a new schema field on `venues`, and new enforcement logic added to two existing mutations. Task 53 is the highest-risk piece — it adds a check to `createBooking`, the exact mutation R-4's atomicity guarantee governs, so the new lead-time check must live inside the same mutation as the existing conflict/block checks, never a separate call. Do Task 50 anytime (fully independent). Within the settings group, strict order matters: 51 (infrastructure) → 52 (lowest-risk swap) → 53 (new enforcement, needs care) → 54 (schema change, largest blast radius, benefits from the other three settings already working end-to-end as a proven pattern).

## How to update this file

When a phase's tasks (see `TASKS.md`) are all checked off, flip that row to Complete and move the "current phase" pointer below. Don't mark a phase Complete if its acceptance criteria in `TASKS.md` haven't been individually verified — task-complete and acceptance-criteria-met are not the same thing.

**Current phase:** Phases 12, 13, and 14 are all now complete — no open code-fixable defects from that work remain (see Phase 14's row for the 43a/44a/44b fix history). **Phase 15** delivered real sidebar destinations for `cancelBooking`, `createCourtBlock`/`removeCourtBlock`, and `setVenueSuspended`/`setUserSuspended` (Tasks 46–49), including the priority self-suspension safety fix (Task 49a, fully verified). **Task 47b** (finishing one missed pending-state control) is the only item left in Phase 15, and it's **on hold — Codex's usage limit is exhausted until 2026-09-21.**

**Phase 16 — Complete Superadmin Dashboard** was planned (2026-09-20) against `SPEC.md` §1's actual superadmin story, then implemented the same day directly by Claude (not Codex — user's explicit instruction "you execute 50-54", since Codex's usage quota was exhausted) — see `REVIEW.md`'s Tasks 50-54 entry for full acceptance-criteria verification and the process-deviation caveat (self-reviewed, worth a second look once Codex quota resets, especially around `createBooking`/`cancelBooking`'s R-4 atomicity). All five tasks' acceptance criteria are met: a platform-wide `listAllBookings` view (Task 50), a `platformSettings` singleton with `getPlatformSettings`/`updatePlatformSettings` (Task 51), the cancellation window (Task 52) and booking lead-time (Task 53, closing a real previously-uncaught server-side gap) now read from settings instead of hardcoded constants, and venue submission now requires and validates a supported city (Task 54). `npm test` (113/113) and `npm run build` both pass. **Phase 16 is complete.** Task 47b remains **on hold — Codex's usage limit is exhausted until 2026-09-21** — that's the only item left in Phase 15, unaffected by this work. Everything else outstanding is external: Hostinger credentials (Phase 0, Phase 6, Task 12a criterion 4, and the final live-auth check) and physical-device PWA install testing (Phase 5, R-5).

Phases 0–2 and 4–11 are now complete or blocked only externally, with every concrete defect found across ten independent review passes fixed the same day it was found. **No open code-fixable defects remain anywhere in the project.** What's left: Hostinger credentials (Phase 0, Phase 6, Task 11, Task 12a criterion 4, and the final live-auth check against `badmintul.com`) and physical-device PWA install testing (Phase 5, R-5) — both external-access blockers with no code fix available.
